# Nim compiler configuration for nomos-da wrapper

# Path configuration
path="$projectPath/src"

# Link against the Rust FFI library
# The library is built in ffi-wrapper/target/release
passL:"-L$projectPath/ffi-wrapper/target/release"
passL:"-lnomos_da_ffi"

# Compiler options
define:danger
opt:size

# C compiler options for FFI (if needed)
# passC:"-I$projectPath/ffi-wrapper/include"
